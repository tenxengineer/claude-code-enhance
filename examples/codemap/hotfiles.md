# Hot Files — <Project Name>

Top files by 90-day change frequency. **Where 80% of recent activity
happens = where 80% of new bugs hide.** Extra care when touching anything
in this list.

> This file is AUTO-GENERATED by the `/refresh-codemap` skill. Run it
> weekly. The skill regenerates the table from git history and prompts
> you to annotate each row. You can edit annotations any time; they
> survive regeneration.

Generated: YYYY-MM-DD

| Changes | File                           | What it is                                 |
| ------- | ------------------------------ | ------------------------------------------ |
| 25      | `apps/.../some-hot-file.ts`    | [annotation: what is this, why is it hot?] |
| 18      | `apps/.../another-hot-file.ts` | [annotation]                               |
| ...     |

## Patterns to notice

[A paragraph or two summarizing what the hot-files cluster tells you.
What subsystem is dominantly active? What does that imply?]

## When designing changes

1. Check whether the file you're touching is in this list.
2. If yes — open it, scan the last 5 commits with `git log -p -5 <file>`
   to understand recent direction before you patch.
3. If you're about to introduce a new file in an area dominated by
   existing hot files, check whether the existing files already have
   the abstraction you're about to create.
